<!-- userAndCats.1.username goes to the first item in the array -->

<h1>{{users.0.username}}'s Profile</h1>

<p>id in system: {{users.0.id}}</p>
<p>Email: {{users.0.email}}</p>

<!-- <h2>users that {{users.0.username}} owns</h2> -->

<!-- <ul>
  {{#each userAndCats}}
    <li>
      <p>Cat's name: {{name}}</p>
      <p>Is this cat sleepy?
        {{#if sleepy}}
          yes
        {{else}}
          no
        {{/if}}
      </p>
    </li>
  {{/each}}
</ul> -->
